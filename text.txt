working directory → stagging area  → local repository  → remote repository

test-projects     →  git add........→ git commit .......→ git push

tracked 
untracked
make changes -->git add .
-->git commit -m "message" -->git push
git pull